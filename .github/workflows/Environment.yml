name: Environment
on:
  workflow_dispatch:

jobs:
  use-dev-secret:
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - run: echo "DEV-API-KEY present: ${{ secrets.DEV-API-KEY != '' }}"
        # do NOT print the value — just whether it resolved
